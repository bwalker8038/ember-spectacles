<h1>Foo</h1>

<Camera @constraints={{model.constraints}} as |c|>
  <c.viewFinder />
  <c.zoomSlider />
  <c.sensorToggle>Toggle Camera</c.sensorToggle>
  <c.shutter @captureImage={{this.captureImage}}>
    {{Take Picture}}
  </c.shutter>
</Camera>
{{outlet}}